# v1.1 Baseline

Baseline captured from `master` commit `b000230` before the Windows and Docker platform refactor.

- Application version: `1.1.0`
- Test runner: Vitest 4.1.0
- Baseline result: 16 test files and 79 tests passed
- TypeScript node and web typechecks passed
- Electron main, preload, and renderer production builds passed
- Baseline fixtures preserve the unversioned v1.1 settings and queue shapes for migration tests

The baseline was recorded on Windows from the clean `v1.1` tag. Later milestones must retain these behaviors or migrate them explicitly.
